# ✅ ToDo List — React App

A clean and minimal **To-Do List** web application built with **React 19**. Users can add, complete, and delete tasks with a simple and responsive UI.



## ✨ Features

- ➕ **Add Tasks** — Quickly add new to-do items
- ✅ **Mark as Complete** — Toggle tasks between done and pending
- 🗑️ **Delete Tasks** — Remove tasks you no longer need
- 💾 **State Management** — Managed with React hooks (`useState`)
- 📱 **Responsive Design** — Works across all screen sizes

---

## 🛠️ Tech Stack

| Technology | Purpose |
|---|---|
| React 19 | UI library |
| React Hooks (`useState`) | State management |
| JavaScript (ES6+) | App logic |
| CSS3 | Custom styling |
| HTML5 | Markup |
| Create React App | Project bootstrapping |

---

## 📁 Project Structure

```
ToDo_React_Project/
├── public/
│   └── index.html         # HTML entry point
├── src/
│   ├── components/        # Reusable React components
│   ├── App.js             # Root component
│   ├── App.css            # App-level styles
│   ├── index.js           # React DOM entry point
│   └── index.css          # Global styles
├── .gitignore
├── package.json
└── README.md
```

---

## ⚙️ Getting Started

### Prerequisites

Make sure you have the following installed:

- [Node.js](https://nodejs.org/) (v16+)
- [Git](https://git-scm.com/)

### Installation

1. **Clone the repository**
   ```bash
   git clone https://github.com/sangamithra-s/ToDo_React_Project.git
   cd ToDo_React_Project
   ```

2. **Install dependencies**
   ```bash
   npm install
   ```

3. **Start the development server**
   ```bash
   npm start
   ```

4. **Open in your browser**
   ```
   http://localhost:3000
   ```

---

## 📜 Available Scripts

| Script | Description |
|---|---|
| `npm start` | Runs the app in development mode |
| `npm test` | Launches the test runner |
| `npm run build` | Builds the app for production |
| `npm run eject` | Ejects from Create React App |

---

## 🚀 Deployment

This app can be deployed for free on:

- **Vercel** — Connect your GitHub repo and deploy in seconds
- **Netlify** — Drag and drop the `build/` folder or connect via GitHub
- **GitHub Pages** — Use `gh-pages` package to deploy

To create a production build:
```bash
npm run build
```

---

## 🧠 What I Learned

- Building functional components with React 19
- Managing state using the `useState` hook
- Handling user events (add, toggle, delete)
- Writing clean and reusable React components
- Styling a React app with custom CSS

---

## 🔮 Future Improvements

- [ ] Add local storage persistence so tasks survive page refresh
- [ ] Add task categories / priority levels
- [ ] Add drag-and-drop to reorder tasks
- [ ] Add due dates and reminders
- [ ] Deploy on Vercel / Netlify

---

## 📄 License

This project is open source and available under the [MIT License](LICENSE).

---

## 👩‍💻 Author

**Sangamithra S**
[GitHub](https://github.com/sangamithra-s)
